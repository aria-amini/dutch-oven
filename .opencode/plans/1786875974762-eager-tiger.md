# Fix 500 "HTTPError" on /

## Diagnosis (complete)

- Dev logs (`pitchfork logs dev`) show the real error:
  `Cannot find module '@/lib/pantry/server' imported from 'src/routes/_app/pantry.tsx'`
  thrown by the Vite+ module runner as `ERR_MODULE_NOT_FOUND`, surfaced as
  `{"status":500,"unhandled":true,"message":"HTTPError"}`.
- The module exists on disk: `src/lib/pantry/server.ts`, and the import in
  `src/routes/_app/pantry.tsx:13-17` is correct. The vite+ module-runner cached
  a negative resolution (server started before the file existed, or stale cache
  after the pantry feature was added in this working copy).
- The daemon was already restarted at 06:27:44 and
  `curl https://dutch-oven.lvh.ariaamini.com/` now returns **200**. No code
  change is needed.

## Plan

1. If the error is still visible to the user (cached client-side or recurring):
   restart the dev daemon to clear the module-runner cache:
   `pitchfork restart dev` (or `pitchfork stop dev && pitchfork start dev` —
   check `pitchfork --help`; daemon auto-starts on directory entry).
2. Sanity check: `vp check` to confirm the pantry module passes lint/type-check
   (rule out a real resolution/config issue with the `@` alias).
3. If the module-not-found recurs after restart, clear Vite cache:
   `rm -rf node_modules/.vite` (or the vite+ cache dir) and restart again.

## Critical files

- `src/routes/_app/pantry.tsx` (imports `@/lib/pantry/server` at line 17)
- `src/lib/pantry/server.ts` (exists; no edit expected)
- `pitchfork.toml` (daemon config)

## Verification

- `curl -sk -o /dev/null -w "%{http_code}\n" https://dutch-oven.lvh.ariaamini.com/`
  → 200
- `pitchfork logs dev | tail` → no `ERR_MODULE_NOT_FOUND` after restart
- Load `/pantry` (authed) and confirm `listPantry` server function works
- `vp check` passes
